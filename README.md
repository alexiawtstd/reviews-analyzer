# Умный консультант по подбору товаров

Веб-сервис для анализа тональности отзывов с сайта iRecommend.com на основе искусственного интеллекта.

## Описание

Сервис анализирует отзывы о товарах с сайта iRecommend.com, определяет их тональность (положительные, нейтральные, негативные) и предоставляет пользователю детальную статистику и общую оценку товара.

## Функциональность

- 🔐 Система авторизации и регистрации пользователей
- 🔍 Парсинг отзывов с сайта iRecommend.com
- 🤖 Анализ тональности отзывов с использованием модели `blanchefort/rubert-base-sentiment`
- 📊 Визуализация результатов анализа (процентное соотношение, общая оценка)
- 📝 История всех проведенных анализов
- 👤 Профиль пользователя с сохранением истории

## Технологии

- **Backend**: Flask (Python)
- **База данных**: SQLite (SQLAlchemy)
- **Анализ тональности**: Transformers (blanchefort/rubert-base-sentiment)
- **Парсинг**: BeautifulSoup4, Requests
- **Frontend**: HTML, CSS, JavaScript

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта:
```
SECRET_KEY=your-secret-key-here
```

4. Запустите приложение:
```bash
python app.py
```

5. Откройте браузер и перейдите по адресу: `http://localhost:5000`

## Использование

1. Зарегистрируйтесь или войдите в систему
2. Перейдите на страницу "Анализ"
3. Вставьте ссылку на товар с сайта iRecommend.com
4. Нажмите "Анализировать отзывы"
5. Просмотрите результаты анализа
6. Все анализы сохраняются в вашем профиле

## Структура проекта

```
.
├── app.py                 # Основное приложение Flask
├── requirements.txt       # Зависимости проекта
├── README.md             # Документация
├── .env                  # Переменные окружения (создать вручную)
├── templates/            # HTML шаблоны
│   ├── base.html
│   ├── index.html
│   ├── login.html
│   ├── instruction.html
│   ├── results.html
│   └── profile.html
└── static/               # Статические файлы
    └── css/
        └── style.css
```

## Развертывание на облачных платформах

### Важно: GitHub Pages не поддерживает Flask

GitHub Pages работает только со статическими сайтами. Для Flask-приложений используйте следующие платформы:

### Вариант 1: Render.com (Рекомендуется - бесплатный тариф)

1. **Создайте аккаунт на [Render.com](https://render.com)**

2. **Подключите GitHub репозиторий:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите ветку (обычно `main`)

3. **Настройте сервис:**
   - **Name**: reviews-analyzer (или любое другое)
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`

4. **Добавьте переменные окружения:**
   - `SECRET_KEY` - сгенерируйте случайный ключ (можно использовать: `python -c "import secrets; print(secrets.token_hex(32))"`)

5. **Нажмите "Create Web Service"**

6. **Примечание:** Первый запуск может занять 10-15 минут из-за загрузки модели ML

### Вариант 2: Railway.app

1. **Создайте аккаунт на [Railway.app](https://railway.app)**

2. **Подключите GitHub:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - Добавьте `SECRET_KEY` в разделе Variables

4. **Railway автоматически определит настройки из `railway.json`**

### Вариант 3: Heroku

1. **Установите Heroku CLI:**
   ```bash
   # Windows: скачайте с heroku.com
   # Mac: brew install heroku/brew/heroku
   ```

2. **Войдите в Heroku:**
   ```bash
   heroku login
   ```

3. **Создайте приложение:**
   ```bash
   heroku create your-app-name
   ```

4. **Добавьте переменные окружения:**
   ```bash
   heroku config:set SECRET_KEY=your-secret-key-here
   ```

5. **Загрузите код:**
   ```bash
   git push heroku main
   ```

6. **Откройте приложение:**
   ```bash
   heroku open
   ```

### Вариант 4: PythonAnywhere

1. **Создайте аккаунт на [PythonAnywhere.com](https://www.pythonanywhere.com)**

2. **Загрузите код через Git:**
   - В разделе "Files" → "Consoles" → "Bash"
   - Клонируйте репозиторий: `git clone https://github.com/yourusername/your-repo.git`

3. **Настройте веб-приложение:**
   - Перейдите в раздел "Web"
   - Выберите "Add a new web app"
   - Выберите Flask и Python 3.10
   - Укажите путь к `app.py`

4. **Настройте переменные окружения в файле конфигурации**

### Алгоритм развертывания (общий):

1. **Загрузите код на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/yourusername/your-repo.git
   git push -u origin main
   ```

2. **Выберите платформу** (Render рекомендуется для начала)

3. **Подключите репозиторий** к выбранной платформе

4. **Настройте переменные окружения** (особенно `SECRET_KEY`)

5. **Дождитесь завершения деплоя** (первый раз может занять время из-за загрузки модели)

6. **Проверьте работу** приложения по предоставленному URL

### Важные замечания для деплоя:

- ⚠️ **Модель ML большая** - первый деплой может занять 10-20 минут
- ⚠️ **SQLite на облачных платформах** - данные могут теряться при перезапуске (для продакшена лучше использовать PostgreSQL)
- ⚠️ **Бесплатные тарифы** имеют ограничения по памяти и времени работы
- ✅ **SECRET_KEY** должен быть уникальным и секретным в продакшене

## Примечания

- При первом запуске модель анализа тональности будет загружена автоматически (это может занять некоторое время)
- База данных SQLite создается автоматически при первом запуске
- Сервис анализирует первые 20 отзывов с указанной страницы товара
- Для продакшена рекомендуется использовать PostgreSQL вместо SQLite

## Лицензия

Этот проект создан в образовательных целях.

